const mongoose = require('mongoose');

const productSchema = new mongoose.Schema({
  name: {
    type: String,
    required: true,
  },
  description: {
    type: String,
    required: true,
  },
  price: {
    type: Number,
    required: true,
  },
  category: {
    type: String,
    required: true,
  },
  // This stores the mathematical meaning of the product description generated by Gemini.
  embedding: {
    type: [Number],
    required: false,
  },
  imgUrl: {
    type: String,
    required: false,
  },
  sellerId: {
    type: String,
    default: 'Orbit Official',
  },
  stockQuantity: {
    type: Number,
    default: 100, // We pretend every item has 100 units in stock for now
  },
  salesCount: {
    type: Number,
    default: 0,
  }
}, { timestamps: true });

const Product = mongoose.model('Product', productSchema);

module.exports = Product;
